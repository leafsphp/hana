{
  "name": "@hanabira/config",
  "version": "0.1.0",
  "files": [
    "eslint",
    "tsconfig"
  ],
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -R tsconfig dist && cp -R eslint dist",
    "release": "pnpm publish"
  },
  "dependencies": {
    "@changesets/cli": "^2.26.2",
    "next": "^13.4.12"
  },
  "devDependencies": {
    "@types/node": "^18.11.17",
    "tsup": "^6.1.3"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  }
}
